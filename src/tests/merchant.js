import Merchant from '../models/merchant'

export default async function test() {
  /*
  const cashCash = await Merchant.findOne({ delivery: true })

  console.log('\n', cashCash, '\n')
  console.log('\n', cashCash.delivers, '\n')
  console.log('\n', cashCash.rate('USD'), '\n')
  console.log('\n', cashCash.rate('USD').sell, '\n')

  console.log('\ndelivery merchants:\n', await Merchant.deliver())
  console.log(
    '\ncharging delivery merchants:\n',
    await Merchant.deliver().charging(),
  )

  const [lng, lat] = [34.7768717, 32.0578385]
  const maxDistance = 2
  const maxResults = 2
  console.log('\nfindNearest results:\n')
  console.log(
    await Merchant.findNearest({ lat, lng, maxDistance, maxResults }).catch(
      err => console.log(err),
    ),
  )
  */
}
